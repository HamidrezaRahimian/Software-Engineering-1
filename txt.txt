git checkout -b main  # Create a new branch named 'main'
touch README.md       # Add a simple file (like README.md)
git add README.md
git commit -m "Initial commit"  # Commit the changes
git push -u origin main         # Push to GitHub
